name: Maki Dice
description: A dice roller for tabletop role-playing games.
publish_to: 'none'
version: 1.9.3+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.5.2

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
