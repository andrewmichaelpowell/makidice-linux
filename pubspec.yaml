name: maki_dice
description: A dice roller for tabletop role-playing games.
publish_to: 'none'
version: 1.9.3+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
